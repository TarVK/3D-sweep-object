import {IJSONValidator} from "./_types/IJSONValidator";

/**
 * Checks whether validator a and b are equivalent, based on their identifier
 * @param a Validator a to be checked
 * @param b Validator b to be checked
 * @returns Whether a and b are equivalent
 */
export function validatorsEqual(a: IJSONValidator, b: IJSONValidator): boolean {
    // TODO:
    return false;
}
